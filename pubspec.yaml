name: motivation_pro
description: App de motivación personal con IA para Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI y Material Design
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  
  # State Management
  provider: ^6.1.1
  
  # Base de datos local
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # Almacenamiento simple
  shared_preferences: ^2.2.2
  
  # HTTP y APIs
  http: ^1.1.2
  dio: ^5.4.0
  
  # Notificaciones
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  
  # Permisos
  permission_handler: ^11.1.0
  
  # Compartir
  share_plus: ^7.2.1
  
  # Paths de archivos
  path_provider: ^2.1.1
  
  # JSON
  json_annotation: ^4.8.1
  
  # Fechas
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true